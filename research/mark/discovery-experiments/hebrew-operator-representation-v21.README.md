# Hebrew operator representation v21

V21 holds the V20 anonymous four-history state and base-lemma consequence target fixed while varying only Hebrew operator identity: lemma, lemma + coarse OSHB morphology, and lemma + full aligned OSHB morphology.

A morphology-refined representation counts as a rescue only if it transfers in both holdout and control and also beats the inherited lemma representation on paired parent lemmas in both lanes. Cross-system structural-context alignment remains secondary and uses no Hebrew↔glyph operator pairing.
