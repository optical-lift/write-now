# Independent Source Law Discovery v1 — Blind Cross-Source Assessment

Status: **FROZEN BEFORE PROVENANCE REJOIN**
Date: **2026-09-19**

Nine anonymous source catalogues were frozen before any cross-source matching.

The catalogue freeze manifest is:

`2d55e927ec31360c5e8ef1e32914f9b4c9d44296f01ec047d6d56805ac612482`

All nine carry `INDEPENDENT_LOCAL_DISCOVERY` custody.

## First correction: 34 pairwise admissions are not 34 laws

Eight sources admitted four pairwise constraints and one source admitted two.

The local evaluator reports each binary skew twice:

- enrichment of outcome A;
- exclusion of outcome B.

Because the resolved outcome alphabet for these contexts is binary, those are complementary measurements of the same contrast.

They must not be counted as independent laws.

## Blind local pattern

Eight of nine sources independently recover both of these source-local contrasts:

1. with a junction center and a context arm that runs to an endpoint, the co-arm is enriched toward endpoint and excluded away from junction relative to the source-local rearrangement null;
2. with a junction center and a context arm that runs to another junction, the co-arm is enriched toward junction and excluded away from endpoint.

Under the frozen Law-Equivalence Adjudication Language these two contrasts are `SAME_LAW` under endpoint↔junction state relabeling.

The complete relational dependency is:

> **At a junction, when one arm carries destination-state X, the co-arm destination is constrained toward matching X rather than behaving as a free reassignment under the same local component inventory.**

This is `ISLD-LAW-001`.

It was recovered independently in **8 of 9 sealed source objects**.

## The ninth source is not discarded

`S6E5A315EA8D70272` does not pass the endpoint-context relation.

More importantly, under the junction-context condition it passes the opposite constraint:

- junction→endpoint is enriched;
- junction→junction is excluded.

That is `CONTRADICTORY` to ISLD-LAW-001 under the currently measured normalized condition.

The correct interpretation is not “ignore the outlier” and not “the law is universal anyway.”

It means:

> the current condition description is incomplete for at least one source, or the larger system contains a genuine lawful branch we have not represented yet.

This is exactly the kind of exception the whole-system method was built to preserve.

## What has now changed

For the first time in the roadmap, the project has a real independent-discovery count:

- independently frozen source catalogues: **9**
- sources independently recovering the same normalized law: **8**
- source with a contradictory local law under the current condition: **1**

These are not heldout transfers and not repeated scores from one shared model.

Each catalogue was generated using only that source's own physical evidence and its own structure-preserving null worlds.

## What remains sealed

At this freeze:

- source provenance is still sealed;
- prior source-rule-atlas incidence is still sealed;
- EQC/MCR labels are still sealed;
- canon is still sealed.

The next legitimate opening is provenance rejoin and condition diagnosis for the contradictory source, followed only afterward by comparison to previously frozen global-law and canon lanes.
