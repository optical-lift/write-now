# Independent Source Law Discovery v1 — Blind Cross-Source Equivalence

Status: **FROZEN BEFORE PROVENANCE OR PRIOR-LAW OPENING**
Date: **2026-09-18**

The nine source-local catalogue hashes were frozen before this comparison.

## Binary-complement normalization

The raw catalogues preserve **34 admitted context→outcome rows**.

Because the measured junction outcome space is binary, paired enrichment/exclusion statements within the same source and same context are algebraically complementary. They normalize to **17 binary relational constraint units** without changing the raw catalogue.

This is not a deletion: both raw rows remain frozen in the workflow artifact and catalogue hashes.

## ISLD-BC001 — eight independent sources

Measured condition:

`CENTER:JUNCTION | context arm = PATH_TO_ENDPOINT`

Constraint:

> the paired arm is enriched toward `PATH_TO_ENDPOINT` and excluded away from `PATH_TO_JUNCTION` relative to the source's own structure-preserving rearrangements.

Independent source count: **8**.

The same complete measured dependency recurs in all eight, so these are adjudicated `SAME_LAW` with one another.

## ISLD-BC002 — eight independent sources

Measured condition:

`CENTER:JUNCTION | context arm = PATH_TO_JUNCTION`

Constraint:

> the paired arm is enriched toward `PATH_TO_JUNCTION` and excluded away from `PATH_TO_ENDPOINT`.

Independent source count: **8**.

Again, the complete measured dependency is the same across those eight sources.

## BC001 is not silently collapsed into BC002

Both constraints have a common pattern—paired arms favor the same measured arm class—but the context arm itself is a relevant condition.

Under the frozen strict-condition rule:

`ISLD-BC001 ↔ ISLD-BC002 = DISTINCT_LAW_SHARED_PATTERN`

This deliberately preserves the user's requirement that different relevant conditions remain different laws even when a deeper symmetry is visible.

## ISLD-BC003 — one independent source

Source:

`S6E5A315EA8D70272`

Measured condition is the **same as BC002**:

`CENTER:JUNCTION | context arm = PATH_TO_JUNCTION`

But the consequence is reversed:

> paired arm enriched toward `PATH_TO_ENDPOINT` and excluded away from `PATH_TO_JUNCTION`.

Therefore, at the currently measured condition:

`ISLD-BC002 ↔ ISLD-BC003 = CONTRADICTORY`

That contradiction is scientifically important.

It means at least one of two things must eventually be true:

1. the larger system genuinely permits a source-local opposite law under some condition; or
2. our current condition signature is incomplete and a missing source-local variable determines which branch applies.

The experiment does **not** choose between those explanations yet.

## Additional pressure from the ninth source

In the same source, the endpoint-conditioned pair trends in the complementary heterotypic direction too, but misses the frozen all-64-null gate by exactly one null world.

That result remains a diagnostic, not a law.

## What is established before provenance opening

For the first time in the project we have:

- **8 independently discovered occurrences** of BC001;
- **8 independently discovered occurrences** of BC002;
- **1 independently discovered contradictory occurrence** under the BC002 measured condition;
- no imported EQC/MCR/canon labels;
- no global trained law model;
- no provenance-based source selection.

This clears the historical problem where independent-discovery count was zero.

It does **not** yet establish universality.

The next opening is source provenance and then comparison against the already-frozen prior law/canon lanes.
