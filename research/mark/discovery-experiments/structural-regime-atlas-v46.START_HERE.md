# START HERE — Mark Structural Regime Atlas v46

Repository: `optical-lift/write-now`  
Branch: `mark-structural-regime-atlas-v46`

V46 begins from the closed V45 result but is **not** a continuation whose job is to prove V45 semantic.

Read completely:

1. `structural-regime-atlas-v46.preregistration.md`
2. `structural-regime-atlas-v46.protocol.json`
3. `structural-regime-atlas-v46.partition-freeze.json`
4. `song-composition-harvest-v45.phase-a.freeze.md`
5. `song-composition-harvest-v45.postfreeze-provenance-rejoin.md`

## First implementation action

Audit the existing frozen Source Rule Atlas / V5 / V45 machinery for the observable families listed in the preregistration.

Implement the **smallest disk-bounded feature compiler** that can emit one source-blind structural feature record per untouched observation.

Do not begin by clustering.

Before any regime outcome is inspected:

- commit the feature compiler;
- record exact implementation SHA and file blobs;
- verify the 435-observation exclusion;
- verify all three source partitions and hashes;
- record which target feature families are actually measurable;
- freeze normalization and missing-value rules;
- freeze the discovery-lane artifact SHA.

Only after that implementation freeze may adaptive regime discovery begin.

## Discovery is allowed to learn

Unlike V45, V46's discovery lane is intentionally exploratory.

You may compare multiple unsupervised structural representations on **discovery only**.

Every attempted representation must remain in the record.

Do not open validation until a regime representation and assignment rule are frozen.

Do not open confirmation until discovery/validation selection is closed.

Do not open provenance meanings until confirmation and the structural atlas are frozen.

## What success means

Success is not "writing separates from noise."

Success is learning a reproducible structural distinction that tells us what experiment to build next.

A universal substrate result, a higher-order-program result, a cross-scale result, or a clean representation failure can all be successful V46 outcomes.
