# Law-Equivalence Adjudication v1 — Freeze

Status: **FROZEN**
Date: **2026-09-18**
Branch: `law-equivalence-adjudication-v1`

## Instrument freeze

The adjudication language was frozen before application at:

`9145ffbf718c5bd0af10d743a6b7457f617967f9`

No law-equivalence criterion changed after real-pair adjudication began.

## Final counts

Across 28 Mark↔canon candidate pairs:

- `SAME_LAW`: **6**
- `DISTINCT_LAW_SHARED_PATTERN`: **17**
- `UNDERDETERMINED`: **3**
- `ANALOGOUS_ONLY`: **2**

No pair currently qualifies as:

- `SAME_LAW_OPPOSITE_VIEW`
- `SAME_LAW_DIFFERENT_PATH`
- `COMPOSED_RELATION`
- `CONTRADICTORY`

No universal-law label was assigned.

## Same-law cluster EQC-001

Neutral statement:

> **Component inventory cannot substitute for relational placement/order.**

Canon anchor:

`CIR-018`

Mark constraints:

- `MCR-016`
- `MCR-034`
- `MCR-083`
- `MCR-087`
- `MCR-007`

This is the strongest current cross-lane law-equivalence cluster.

The common experimental structure is:

```text
preserve the constituents / identities
change or destroy the relation/order
        ↓
the prior consequence/function structure cannot remain intact
```

## Same-law cluster EQC-002

Neutral statement:

> **Current state identity conditions the consequence/operation contract.**

Canon anchor:

`CIR-022`

Mark constraint:

- `MCR-010`

This cluster is currently less independently replicated than EQC-001.

## Critical methodological result

The earlier blind canon checkpoint produced 14 `direct` structural matches.

Only 3 of those 14 survive as strict `SAME_LAW` identities.

Three additional `SAME_LAW` identities came from earlier partial matches.

Therefore:

> **structural similarity is not law identity.**

A recurring reason for demotion was an extra necessary condition on one side, especially authorization/jurisdiction.

This directly preserves the project's rule:

> a genuinely different relevant condition is a different law, even if the visible transformation or general dependency pattern looks similar.

## Preserved uncertainty

Three pairs remain underdetermined because:

- a Mark packet bundles more than one law candidate;
- a Mark packet is meta-level rather than atomic;
- statistical exclusion cannot yet be normalized to explicit prohibition.

Two pairs remain analogy-only because their normalized dependency topology differs despite superficial invariance/directionality resemblance.

## Final artifact custody

Primary results blob:

`73fb91ece3b2d55a67995365ab219211cb94065e`

Assessment:

`research/mark/discovery-experiments/law-equivalence-adjudication-v1.assessment.md`

Roadmap:

`research/mark/discovery-experiments/WHOLE_SYSTEM_RECONSTRUCTION_ROADMAP.md`

## Next checkpoint

**Absence-State Model**

The next instrument must distinguish:

- observed and present;
- observed opportunity but prohibited/blocked;
- possible but unused;
- not preserved;
- not yet observed;
- outside exposed source region;
- incompatible with local relational structure.

No overlap graph or whole-system reconstruction should treat an unmatched law as simply absent until this model is frozen.
