import fs from "node:fs/promises";
import { createReadStream } from "node:fs";
import crypto from "node:crypto";

const outDir = process.env.MARK_V7_OUT ?? "artifacts/mark-v7-sparse";
const maxRssKb = Number(process.env.MARK_V7_MAX_RSS_KB ?? 524288);
const summary = JSON.parse(await fs.readFile(`${outDir}/summary.json`, "utf8"));
const custody = JSON.parse(await fs.readFile(`${outDir}/custody.json`, "utf8"));
const evaluation = JSON.parse(await fs.readFile(`${outDir}/evaluation.json`, "utf8"));
const runtime = await fs.readFile(`${outDir}/runtime.txt`, "utf8");
if (summary.schema !== "mark_sparse_compiler_summary_v1") throw new Error(`unexpected summary schema ${summary.schema}`);
if (custody.schema !== "mark_sparse_ledger_custody_v1") throw new Error(`unexpected custody schema ${custody.schema}`);
if (evaluation.schema !== "mark_sparse_transfer_evaluation_v1") throw new Error(`unexpected evaluation schema ${evaluation.schema}`);
if (!/^[a-f0-9]{64}$/.test(custody.merkleRoot)) throw new Error("invalid ledger Merkle root");
if (summary.ledgerMerkleRoot !== custody.merkleRoot) throw new Error("summary/custody Merkle root mismatch");
if (summary.sources < 3 || summary.observations < 10 || summary.tiles < 1 || summary.events < 1) throw new Error("v7 compiler did not process a meaningful blind corpus");
if (summary.centers < 1) throw new Error("v7 compiler emitted no relational centers");
if (BigInt(summary.observedPairWeight) < 1n) throw new Error("v7 compiler emitted no maskable relational pair weight");
if (!custody.contract?.appendOnlyEventLedger || custody.contract?.wholeWorldGraphMaterialized || custody.contract?.wholeWorldJsonMaterialized || custody.contract?.grammarPairsMaterialized) throw new Error("v7 sparse custody contract was violated");
if (evaluation.rules < 1) throw new Error("v7 training lane discovered no recurrent masked-relation rule");
const rssMatch = runtime.match(/Maximum resident set size \(kbytes\):\s*(\d+)/);
if (!rssMatch) throw new Error("could not read maximum RSS from compiler runtime report");
const maxRssObserved = Number(rssMatch[1]);
if (maxRssObserved > maxRssKb) throw new Error(`v7 compiler exceeded memory gate: ${maxRssObserved} KB > ${maxRssKb} KB`);
const ledgerHasher = crypto.createHash("sha256");
for await (const chunk of createReadStream(`${outDir}/events.jsonl`)) ledgerHasher.update(chunk);
const ledgerSha256 = ledgerHasher.digest("hex");
await fs.writeFile(`${outDir}/assertion.json`, `${JSON.stringify({schema:"mark_sparse_architecture_assertion_v1",status:"passed",sourceBlindInputSha256:summary.sourceBlindInputSha256,sources:summary.sources,observations:summary.observations,centers:summary.centers,events:summary.events,rules:evaluation.rules,maxRssKb:maxRssObserved,memoryGateKb:maxRssKb,ledgerMerkleRoot:custody.merkleRoot,ledgerFileSha256:ledgerSha256,scientificInterpretationAuthorized:false}, null, 2)}\n`);
console.log(`Mark v7 sparse architecture gate passed: ${summary.sources} sources, ${summary.observations} observations, ${summary.events} events, ${evaluation.rules} rules, max RSS ${maxRssObserved} KB`);
