name: Mark Typed Relational Intervention v2

on:
  pull_request:
    paths:
      - "research/mark/discovery-experiments/typed-relational-intervention-v2.protocol.json"
      - "scripts/test-mark-typed-relational-intervention-v2.py"
      - ".github/workflows/mark-typed-relational-intervention-v2.yml"
      - "scripts/mark_graph_compression_v8_core.py"
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  pull-requests: write

env:
  MARK_TYPED_RELATIONAL_PROTOCOL: research/mark/discovery-experiments/typed-relational-intervention-v2.protocol.json
  MARK_TYPED_RELATIONAL_V5: artifact-staging/typed-relational/v5
  MARK_TYPED_RELATIONAL_V8: artifact-staging/typed-relational/v8
  MARK_TYPED_RELATIONAL_OUT: artifacts/mark-typed-relational-intervention-v2

jobs:
  typed-relational-intervention:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Validate frozen intervention before evidence
        run: |
          python3 -m json.tool "$MARK_TYPED_RELATIONAL_PROTOCOL" >/dev/null
          python3 -m py_compile scripts/mark_graph_compression_v8_core.py scripts/test-mark-typed-relational-intervention-v2.py

      - name: Download frozen V8 blind grammar only
        uses: actions/download-artifact@v4
        with:
          name: mark-hierarchical-graph-compression-v8-grammar-freeze
          path: artifact-staging/typed-relational/v8
          repository: optical-lift/write-now
          run-id: 33821017246
          github-token: ${{ github.token }}

      - name: Download exact frozen V5 graph world
        uses: actions/download-artifact@v4
        with:
          name: mark-critical-edge-correspondence-v5-frozen
          path: artifact-staging/typed-relational/v5
          repository: optical-lift/write-now
          run-id: 33807154154
          github-token: ${{ github.token }}

      - name: Run typed relationship-class intervention
        run: python3 scripts/test-mark-typed-relational-intervention-v2.py

      - name: Publish frozen result
        run: cat "$MARK_TYPED_RELATIONAL_OUT/summary.md" >> "$GITHUB_STEP_SUMMARY"

      - name: Publish result on PR
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: gh pr comment "$PR_NUMBER" --body-file "$MARK_TYPED_RELATIONAL_OUT/summary.md" --edit-last --create-if-none

      - name: Upload frozen intervention packet
        uses: actions/upload-artifact@v4
        with:
          name: mark-typed-relational-intervention-v2-frozen
          path: artifacts/mark-typed-relational-intervention-v2/**
          if-no-files-found: error
          retention-days: 30
